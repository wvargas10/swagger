logging:
  level:
    root: INFO # entire application default debug level
    com.swagger.openapi: DEBUG # package application debug level
  pattern:
    console: "[%d{yyyy-MM-dd HH:mm:ss}] application [%t] [%F:%L] %-5level: %msg%n"

#Spring:
#  profiles:
#    active: local
